{
  "type": "monitor",
  "monitor_type": "query_level_monitor",
  "name": "healthcheck-failure",
  "enabled": true,
  "schedule": {
    "period": {
      "interval": 5,
      "unit": "MINUTES"
    }
  },
  "inputs": [
    {
      "search": {
        "indices": [
          "logs-homelab-*"
        ],
        "query": {
          "size": 0,
          "query": {
            "bool": {
              "must": [
                { "term": { "healthcheck": true } },
                { "range": { "@timestamp": { "gte": "now-5m" } } }
              ],
              "must_not": [
                { "term": { "status": 200 } }
              ]
            }
          },
          "aggs": {
            "by_service": {
              "terms": { "field": "container_name", "size": 10 },
              "aggs": {
                "by_status": { "terms": { "field": "status", "size": 5 } }
              }
            }
          }
        }
      }
    }
  ],
  "triggers": [
    {
      "name": "healthcheck-failed-3",
      "severity": "2",
      "condition": {
        "script": {
          "source": "ctx.results[0].hits.total.value >= 3",
          "lang": "painless"
        }
      },
      "actions": [
        {
          "name": "email-mpcheli",
          "destination_id": "cVtg7p0BAgiY_8mBBS8g",
          "message_template": {
            "source": "{\"personalizations\":[{\"to\":[{\"email\":\"mpcheli7@gmail.com\"}]}],\"from\":{\"email\":\"status@markcheli.com\",\"name\":\"Homelab Alerts\"},\"subject\":\"[homelab] healthcheck failures: {{ctx.results.0.hits.total.value}} in 5 min\",\"content\":[{\"type\":\"text/plain\",\"value\":\"Monitor: {{ctx.monitor.name}} (severity {{ctx.trigger.severity}})\\nWindow: last 5 minutes\\nTotal non-200 healthchecks: {{ctx.results.0.hits.total.value}}\\n\\n=== By service ===\\n{{#ctx.results.0.aggregations.by_service.buckets}}\\n[{{key}}] {{doc_count}} failure(s):{{#by_status.buckets}} status={{key}}({{doc_count}}){{/by_status.buckets}}\\n{{/ctx.results.0.aggregations.by_service.buckets}}\\n=== Investigate ===\\n  docker ps | grep <service>\\n  docker compose logs --tail 100 <service>\\n  curl -fsS http://<service>:<port>/health\\n\\nDashboard: https://logs.ops.markcheli.com/app/dashboards#/view/dash-service-health\"}],\"tracking_settings\":{\"click_tracking\":{\"enable\":false,\"enable_text\":false},\"open_tracking\":{\"enable\":false},\"subscription_tracking\":{\"enable\":false}}}",
            "lang": "mustache"
          },
          "throttle_enabled": true,
          "throttle": {
            "value": 30,
            "unit": "MINUTES"
          }
        }
      ]
    }
  ]
}
