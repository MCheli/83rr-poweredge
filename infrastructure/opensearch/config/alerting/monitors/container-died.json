{
  "type": "monitor",
  "monitor_type": "query_level_monitor",
  "name": "container-died",
  "enabled": true,
  "schedule": { "period": { "interval": 5, "unit": "MINUTES" } },
  "inputs": [
    {
      "search": {
        "indices": ["logs-homelab-*"],
        "query": {
          "size": 0,
          "query": {
            "bool": {
              "must": [
                { "terms": { "event_type": ["die", "kill", "oom"] } },
                { "range": { "@timestamp": { "gte": "now-5m" } } }
              ],
              "must_not": [
                { "terms": { "container_name": ["watchtower", "uptime-kuma"] } }
              ]
            }
          },
          "aggs": {
            "by_container": {
              "terms": { "field": "container_name", "size": 20 },
              "aggs": {
                "by_action": { "terms": { "field": "event_type", "size": 5 } }
              }
            }
          }
        }
      }
    }
  ],
  "triggers": [
    {
      "name": "container-down-event",
      "severity": "1",
      "condition": {
        "script": {
          "source": "ctx.results[0].hits.total.value > 0",
          "lang": "painless"
        }
      },
      "actions": [
        {
          "name": "email-mpcheli",
          "destination_id": "cVtg7p0BAgiY_8mBBS8g",
          "message_template": {
            "source": "{\"personalizations\":[{\"to\":[{\"email\":\"mpcheli7@gmail.com\"}]}],\"from\":{\"email\":\"status@markcheli.com\",\"name\":\"Homelab Alerts\"},\"subject\":\"[homelab] container die/kill/oom: {{ctx.results.0.hits.total.value}} event(s)\",\"content\":[{\"type\":\"text/plain\",\"value\":\"Monitor: {{ctx.monitor.name}} (severity {{ctx.trigger.severity}})\\nWindow: last 5 minutes\\nTotal events: {{ctx.results.0.hits.total.value}}\\n\\n=== By container ===\\n{{#ctx.results.0.aggregations.by_container.buckets}}\\n[{{key}}] {{doc_count}} event(s):{{#by_action.buckets}} {{key}}={{doc_count}}{{/by_action.buckets}}\\n{{/ctx.results.0.aggregations.by_container.buckets}}\\nNote: kill+die in quick succession usually means an operator-initiated restart (docker compose up -d <svc>). A standalone die or oom is more interesting.\\n\\n=== Investigate ===\\n  docker ps -a --filter name=<container>\\n  docker logs --tail 200 <container>\\n  docker inspect <container> | grep -A3 -i exit\\n\\nLifecycle dashboard: https://logs.ops.markcheli.com/app/dashboards#/view/dash-lifecycle\"}],\"tracking_settings\":{\"click_tracking\":{\"enable\":false},\"open_tracking\":{\"enable\":false},\"subscription_tracking\":{\"enable\":false}}}",
            "lang": "mustache"
          },
          "throttle_enabled": true,
          "throttle": { "value": 30, "unit": "MINUTES" }
        }
      ]
    }
  ]
}
