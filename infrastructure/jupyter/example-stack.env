# Example Environment Configuration for JupyterHub Stack
# Copy this file to 'stack.env' and update with your actual values
# NEVER commit stack.env to version control - it should be in .gitignore

# =============================================================================
# SECURITY TOKENS
# =============================================================================

# Configurable HTTP Proxy authentication token
# Generate with: openssl rand -hex 32
CONFIGPROXY_AUTH_TOKEN=your-secure-proxy-token-here

# PostgreSQL password for JupyterHub database
# Generate with: openssl rand -base64 32
POSTGRES_PASSWORD=your-secure-database-password-here

# =============================================================================
# AUTHENTICATION
# =============================================================================

# Dummy authenticator password (for development/testing)
# In production, consider upgrading to OAuth or LDAP
JUPYTER_PASSWORD=your-jupyter-login-password-here

# Admin user for JupyterHub (has administrative privileges)
JUPYTER_ADMIN_USER=your-admin-username-here

# =============================================================================
# DEPLOYMENT CONFIGURATION
# =============================================================================

# Domain name for Traefik reverse proxy
# Example: jupyter.yourdomain.com
JUPYTER_DOMAIN=your-jupyter-domain.example.com

# Timezone for containers
# See: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=America/New_York

# Single-user container image (uses custom pre-built image)
# Custom image includes all packages and extensions pre-installed
# Build with: docker build -t personal-jupyter-singleuser:latest .
SINGLEUSER_IMAGE=personal-jupyter-singleuser:latest

# =============================================================================
# AI ASSISTANCE CONFIGURATION
# =============================================================================

# OpenAI API Key for ChatGPT integration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# Anthropic API Key for Claude integration
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# =============================================================================
# OPTIONAL DATABASE CONFIGURATION
# =============================================================================

# Full database URL (optional - constructed automatically if not provided)
# Format: postgresql://username:password@host:port/database
# JUPYTERHUB_DB_URL=postgresql://jhub:${POSTGRES_PASSWORD}@hub-db:5432/jhub

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 
# 1. Generate strong, unique passwords for all services
# 2. Use different tokens/passwords for each environment (dev/staging/prod)
# 3. Regularly rotate secrets, especially CONFIGPROXY_AUTH_TOKEN
# 4. Consider using Docker secrets or external secret management in production
# 5. Ensure stack.env has restrictive file permissions (600)
#
# Generate secure random values:
# - openssl rand -hex 32    (for tokens)
# - openssl rand -base64 32 (for passwords)
# - pwgen -s 32 1           (alternative password generator)